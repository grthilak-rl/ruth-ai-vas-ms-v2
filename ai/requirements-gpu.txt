# Ruth AI Unified Runtime - GPU Dependencies
# For NVIDIA CUDA-enabled GPU deployments
# Build with: docker build --build-arg VARIANT=gpu -t ruth-ai-runtime:gpu .

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP Client
httpx==0.26.0

# YAML parsing (for model.yaml contracts)
pyyaml==6.0.1

# PyTorch with CUDA support
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121

# Image processing
opencv-python-headless==4.9.0.80
numpy==1.26.3
pillow==10.2.0

# Observability
prometheus-client==0.19.0

# Utilities
python-multipart==0.0.6

# Model-specific dependencies
pandas>=2.0.0
ultralytics>=8.0.0
